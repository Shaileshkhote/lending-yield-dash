{
  "name": "@lendingscope/protocol-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "generate": "node scripts/generate-registry.mjs",
    "build": "pnpm generate && tsc -p tsconfig.json",
    "lint": "pnpm generate && tsc -p tsconfig.json --noEmit",
    "typecheck": "pnpm generate && tsc -p tsconfig.json --noEmit",
    "test": "pnpm typecheck",
    "test:unit": "pnpm typecheck"
  }
}
